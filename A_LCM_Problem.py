from math import gcd
for i in range(int(input())):
        l, r = map(int, input().split())
        
                if l <= l and l*2 <= r:
                        print(l, l*2)
                else:
                print(-1, -1)